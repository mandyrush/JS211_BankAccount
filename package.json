{
  "name": "JS211_Bank-Account",
  "version": "1.0.0",
  "description": "Practicing classes and functions",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mandyrush/JS211_BankAccount.git"
  },
  "scripts": {
    "lint": "htmllint {*.html,./**/*.html} && stylelint ./**/*.css && eslint .",
    "test": "mocha --no-timeouts"
  },
  "author": "Amanda Rush",
  "dependencies": {
    "eslint": "^3.19.0",
    "mocha": "^5.0.0",
    "stylelint": "^7.13.0"
  }
}
